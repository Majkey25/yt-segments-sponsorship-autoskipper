name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build extension
        run: npm run build

      - name: Package extension
        run: npm run package

      - name: Verify release archive root
        shell: bash
        run: |
          unzip -Z1 dist/release/youtube-skipper.zip | grep -Fx "manifest.json"
          test -f dist/extension/background.js
          test -f dist/extension/content.js
          test -f dist/extension/lib/sponsorblock.js
