{
  "name": "youtube-skipper",
  "version": "2.0.0",
  "private": true,
  "description": "Automatically skip sponsors and other community-reported YouTube segments.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
